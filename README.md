# ENS Historical State Time Machine

Monorepo for indexing and reconstructing historical ENS name state on Ethereum.

Packages: `indexer`, `api`, `web`, and `db` (schema and migrations will live under `db/`).
